# =============================================================================
# Local ARTIQ Configuration Overrides
# Phase 3C: Machine-specific settings (NOT TRACKED BY GIT)
# =============================================================================
#
# Copy this file from artiq_config_local.yaml.example and customize
# for your local development machine.
#
# This file overrides values in artiq_config.yaml for this machine only.
# =============================================================================

---
# =============================================================================
# NETWORK OVERRIDES
# =============================================================================
network:
  # Uncomment and modify if ARTIQ is on a different IP
  # master_ip: "192.168.56.101"
  
  # Use localhost if running ARTIQ on same machine (development)
  # master_ip: "127.0.0.1"


# =============================================================================
# PATH OVERRIDES
# =============================================================================
# paths:
#   output_base: "./data"
#   logs: "./logs"


# =============================================================================
# HARDWARE OVERRIDES (for testing without hardware)
# =============================================================================
# hardware:
#   # Disable certain hardware for testing
#   pmt:
#     enabled: false
#   
#   camera:
#     enabled: false


# =============================================================================
# LOGGING OVERRIDES
# =============================================================================
# logging:
#   level: "DEBUG"  # More verbose for development
